{
  "name": "youtube-novel-channel",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply novel_channel --local",
    "db:migrate:remote": "wrangler d1 migrations apply novel_channel --remote"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
